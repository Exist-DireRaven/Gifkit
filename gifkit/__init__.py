"""Gifkit: sprite-sheet to animation-GIF toolkit with motion-compensated in-betweens.

The bundled case configurations reference the Mimo material, whose imagery is
NOT distributed with the repository (local working material only). The walker
example (examples/walker) is fully generative and ships ready to run.
"""
__version__ = "0.2.0"
