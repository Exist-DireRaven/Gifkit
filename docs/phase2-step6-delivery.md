# 第二阶段第六步：开源交付整理

日期：2026-09-13。第二阶段（共六步）至此全部完成。

## 本步做了什么

### README 首屏重写

旧 README 是"版本产物清单 + 越来越长的修复横幅"。新 README 首屏按产品方向
回答三件事：**是什么**（sprite sheet → 流畅循环 GIF，离线核心）、**解决什么**
（光流中割/节奏/配置化/校验内建）、**怎么跑**（pip install -e . + gifkit build）。
能力清单明确分为已实现 / 规划中 / 实验性三档，规划项明确标注"勿当事实引用"。

### 历史内容归档

v1–v4 的产物清单、演进指标、当年"改脚本顶部常量"的工作流说明整体迁入
[docs/mimo-history.md](mimo-history.md)，并在开头注明其命令为历史用法。

### 视觉证据

验证拼图原收录三张至 docs/images/；"素材不发布"决定后，两张 Mimo 拼图改为仅存
本地工作副本（_temp 冒烟目录仍有原件），仓库内保留 walker 深色背景一张（程序化
生成素材，无授权负担）。

### 交付决策（2026-09-13 用户拍板，已实施）

| 事项 | 决定 | 落实 |
|---|---|---|
| 正式命名 | **Gifkit** | pyproject 与 gifkit/ 已去"临时名"标注；PyPI 上 `gifkit` 未被占用（2026-09 查证），项目尚未发布到 PyPI |
| 代码许可证 | **MIT** | LICENSE 文件已加入（含素材除外声明），pyproject `license = MIT` |
| Mimo 素材 | **不发布** | 132 个图像类文件已 `git rm --cached`（本地文件全部保留）并加入 .gitignore；README 改为 walker 主图；依赖素材的测试用例改为自动跳过；同日执行 git-filter-repo 历史重写并强推远端（GitHub 上旧提交在服务端 GC 前仍可经直接 SHA 访问，见 [docs/publishing.md](publishing.md)） |

注：`cases/mimo_*.json` 案例配置只含时序与布局参数（无图像内容），随代码发布；
Mimo 图像类资产（源图/GIF/对齐帧/中割帧/校验拼图/图表）全部只存本地。

## 整个工程的最终状态（截至本步）

- **六步全部完成**：正确性修复 → IO 统一 → 入口拆分 → 配置抽取 → CLI/安装 → 视觉验证+第二素材 → 交付整理。
- **26 个脚本全部 import 安全**；构建/验证全链路配置驱动；72 项自动化测试（约 4 秒，全合成输入）。
- **重建即证据**：五个 Mimo 案例经 `gifkit build` 重建与历史成品指标一致、v4 深色背景逐帧一致；walker 证明换素材零改码。
- **首次视觉验收完成**：动作连贯性、透明边缘、地线稳定性均有目检结论与拼图证据；两个继承性视觉特性（浅色镶边、封闭白底）已定性并记录。

## 遗留事项（均为显式待办，非缺陷）

1. 两个用户决策：正式命名、代码许可证 + Mimo 素材授权（见上表）。
2. 干净环境（无科学计算栈）的 PyPI 依赖解析安装、Python 3.10–3.12 实测。
3. 规划清单中的功能（verify 子命令、APNG/WebP、AI 插件接口、封闭白底检测等）按需求排期。
4. 第一阶段即存在的四个历史成品视觉项——已在本轮第五步全部验收完毕，无遗留。
